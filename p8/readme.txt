assignment requirements

dynamic binding

values for rows and cols will change on slider values, but make sure when the field it self is 
changed by a user the slider updated 

when field or slider is updated, table should regenerate 

when a new table is created put it in a new tab with the control values and table info saved to it
each tab must be labeled with the control variables. 

delete a single or multiple tabs 

pop error messages too you will need to use the validation library
